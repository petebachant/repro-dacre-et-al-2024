dependencies:
  - r # brew install r
  - renv # Rscript -e "install.packages('renv')"
  - xquartz:
      platform: macos
environments:
  r:
    kind: renv
    path: renv.lock
    prefix: renv
datasets:
  - path: RMR_Data_DDacre.xlsx
    title: Data
    description:
